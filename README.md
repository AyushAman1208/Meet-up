## 📘 Project Overview

This application was built while learning **React** for the first time.

It is a simple webpage designed to demonstrate the use of **React Contexts** for state management. The project focuses on understanding core React concepts such as component structure, context providers, and consuming shared state across the component tree.
